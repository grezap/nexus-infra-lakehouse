#!/usr/sbin/nft -f
#
# lakehouse-iceberg-rest-node -- nftables ruleset (Phase 0.L.2 baseline; cold-
# clone safety net). The TF role-overlay-iceberg-nftables-backplane.tf is canon.
#
# nic0 = VMnet11 (clients -> Nessie Iceberg REST :19120; Nessie's outbound to
# the iceberg-db VIP :5432 + minio.nexus.lab is established/related), nic1 =
# VMnet10 (backplane; unused by Nessie but kept uniform with the tier).

flush ruleset

table inet filter {
    chain input {
        type filter hook input priority 0; policy drop;

        iif "lo" accept
        ct state { established, related } accept
        ct state invalid drop
        ip protocol icmp   accept
        ip6 nexthdr icmpv6 accept

        iifname "nic0" ip saddr 192.168.70.0/24 tcp dport 22    accept comment "SSH from VMnet11"
        iifname "nic0" ip saddr 192.168.70.0/24 tcp dport 9100  accept comment "node_exporter from VMnet11"
        iifname "nic1" ip saddr 192.168.10.0/24 accept comment "trusted cluster backplane (VMnet10)"
        iifname "nic0" ip saddr 192.168.70.0/24 tcp dport 19120 accept comment "Nessie Iceberg REST API from VMnet11"

        counter drop
    }
    chain forward { type filter hook forward priority 0; policy drop; }
    chain output  { type filter hook output priority 0; policy accept; }
}
